[to Basic]

IsWorking in Bucket :

	bucket open :

		str Input = sand with 'name : '
		str Value = 'mikaela'

		if Input == Value then :

			do : show 'Hello, Sir!'

		finish

		else then :

			do : show 'You're not mikaela'

		finish

	end l.

close.