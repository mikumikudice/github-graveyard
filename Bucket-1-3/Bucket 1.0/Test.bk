[to Basic]

Test in Bucket :

	bucket open :

		str Input = sand with 'input : '
		convert Input in int

		show 'X = [i]' with Input

		up Input in 2

		show 'X = [i]' with Input

	end l.

close.